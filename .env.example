# Copia este archivo a .env.local y llena los valores reales.
# NUNCA subas el .env.local real a git.

# --- Correo (Resend) — si RESEND_API_KEY está vacía no se envían correos ---
RESEND_API_KEY=""
EMAIL_FROM="IIDEMAYA <no-reply@iidemaya.org.gt>"
# Correo interno que recibe el aviso cuando alguien llena el formulario de contacto.
TEAM_EMAIL=""
